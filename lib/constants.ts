/**
 * Shared constants used across frontend and API routes.
 */

/** Maximum upload file size in bytes (500 MB) */
export const MAX_UPLOAD_SIZE_BYTES = 500 * 1024 * 1024;

/** Maximum upload file size in MB (for display) */
export const MAX_UPLOAD_SIZE_MB = 500;
